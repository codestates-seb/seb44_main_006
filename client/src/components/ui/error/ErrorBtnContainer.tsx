import Text from '../text/Text';

const ErrorBtnContainer = () => {
  return (
    <Text
      styles={{
        color: 'white',
      }}
    >
      메인으로 가기
    </Text>
  );
};

export default ErrorBtnContainer;
