import { css } from 'styled-components';

import cssToken from '../../styles/cssToken';

export const mgpd = css`
  margin-top: 0px;
  padding-top: ${cssToken.SPACING['gap-20']};
  padding-left: ${cssToken.SPACING['gap-20']};
  padding-right: ${cssToken.SPACING['gap-20']};
`;
